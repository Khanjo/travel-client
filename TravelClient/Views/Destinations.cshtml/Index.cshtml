@{
    ViewData["Title"] = "Animals";
}

<h1>All Destinations:</h1>

<ol>
    @foreach (Destination destination in Model)
    {
        <li><strong>@destination.Name</strong> | <a href="/Destinations/Details/@(destination.DestinationId)">View
                Details</a></li>
    }
</ol>